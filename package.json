{
  "private": true,
  "name": "slides-vr-crash-course",
  "version": "0.0.0",
  "description": "Slides for my talk about VR",
  "author": "Denys Dovhan <denysdovhan@gmail.com> (http://denysdovhan.com)",
  "dependencies": {
    "aframe": "^0.6.1",
    "gh-pages": "^1.0.0",
    "husky": "^0.14.3",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.12",
    "spectacle": "^2.2.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && gh-pages -d build -m \"Updated at $(date +'%a, %d %b %Y, %T, %Z')\" --add",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prepush": "npm run deploy"
  }
}
